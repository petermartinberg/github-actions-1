name: sec-workflow

on: pull_request:
    types: [review_requested]
jobs: 
  specific_review_request:
    runs-on: ubuntu-latest
    if: ${{ github.event.review.requested_team.name =='zicken-team'}}
    steps:
      - run: echo 'A review from zicken-team was requested'